"use server";
import { defineFormAction, displayMessage } from "@omer-x/next-form-actions";
import { updateTag } from "next/cache";
import { z } from "zod/v4";
import { delete{{ pascalCase modelName "singular" }}, insert{{ pascalCase modelName "singular" }}, patch{{ pascalCase modelName "singular" }} } from "~/data/mutations/{{ kebabCase modelName "singular" }}";
{{#if i18n}}
import { type AppLocale, redirect } from "~/i18n/routing";
{{else}}
import { redirect } from "next/navigation";
{{/if}}
import { mustBeLoggedIn } from "./middlewares/session";

export const create{{ pascalCase modelName "singular" }} = defineFormAction({
  middlewares: [mustBeLoggedIn],
  schema: schemaOfNew{{ pascalCase modelName "singular" }}DTO,
  action: input => (async (locale: AppLocale) => {
    try {
      await insert{{ pascalCase modelName "singular" }}(input);
    } catch (error) {
      if (error === "XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX") {
        displayMessage("error", error);
      }
      throw error;
    }
    updateTag("getAll{{ pascalCase modelName "plural" }}()");
    return redirect({
      locale,
      href: "/{{ kebabCase modelName "plural" }}"
    });
  }),
});

export const update{{ pascalCase modelName "singular" }} = defineFormAction({
  middlewares: [mustBeLoggedIn],
  schema: schemaOf{{ pascalCase modelName "singular" }}PatchDTO,
  action: input => (async (locale: AppLocale, {{ camelCase modelName "singular" }}Id: {{ pascalCase modelName "singular" }}DTO["id"], fromList: boolean) => {
    try {
      await patch{{ pascalCase modelName "singular" }}({{ camelCase modelName "singular" }}Id, input);
    } catch (error) {
      if (error === "XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX") {
        displayMessage("error", error);
      }
      throw error;
    }
    updateTag(`getSingle{{ pascalCase modelName "singular" }}(${ {{~ camelCase modelName "singular" }}Id})`);
    return redirect({
      locale,
      href: fromList ? "/{{ kebabCase modelName "plural" }}" : {
        pathname: "/{{ kebabCase modelName "plural" }}/[id]",
          params: {
            id: {{ camelCase modelName "singular" }}Id,
          },
        },
      });
  }),
});

export const destroy{{ pascalCase modelName "singular" }} = defineFormAction({
  middlewares: [mustBeLoggedIn],
  schema: z.object({}),
  action: () => (async (locale: AppLocale, {{ camelCase modelName "singular" }}Id: {{ pascalCase modelName "singular" }}DTO["id"]) => {
    try {
      await delete{{ pascalCase modelName "singular" }}({{ camelCase modelName "singular" }}Id);
    } catch (error) {
      if (error === "XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX") {
        displayMessage("error", error);
      }
      throw error;
    }
    updateTag("getAll{{ pascalCase modelName "plural" }}()");
    return redirect({
      locale,
      href: "/{{ kebabCase modelName "plural" }}"
    });
  }),
});
