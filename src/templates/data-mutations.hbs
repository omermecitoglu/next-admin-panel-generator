import "server-only";

export async function insert{{ pascalCase modelName }}(requestBody: New{{ pascalCase modelName }}DTO) {
  const { success, content } = await XXXXXXXXXXXXXXXXXXXXXX(requestBody);
  if ("errorCode" in content) {
    switch (content.errorCode) {
      case "BAD_REQUEST":
      case "UNKNOWN_ERROR":
        throw new Error(content.errorCode);
    }
  }
  if (!success) throw new Error("XXXXXXXXXXXXXXXXXXXXXX was unsuccessful");
  return content;
}

export async function patch{{ pascalCase modelName }}({{ camelCase modelName }}Id: {{ pascalCase modelName }}DTO["id"], requestBody: {{ pascalCase modelName }}PatchDTO) {
  const { success, content } = await XXXXXXXXXXXXXXXXXXXXXX({{ camelCase modelName }}Id, requestBody);
  if ("errorCode" in content) {
    switch (content.errorCode) {
      case "XXXXXXXXXXXXXXXXXXXXXX":
      case "UNKNOWN_ERROR":
        throw new Error(content.errorCode);
    }
  }
  if (!success) throw new Error("XXXXXXXXXXXXXXXXXXXXXX was unsuccessful");
  return content;
}

export async function delete{{ pascalCase modelName }}({{ camelCase modelName }}Id: {{ pascalCase modelName }}DTO["id"]) {
  const { success, content } = await XXXXXXXXXXXXXXXXXXXXXX({{ camelCase modelName }}Id);
  if (content && "errorCode" in content) {
    switch (content.errorCode) {
      case "XXXXXXXXXXXXXXXXXXXXXX":
      case "UNKNOWN_ERROR":
        throw new Error(content.errorCode);
    }
  }
  if (!success) throw new Error("XXXXXXXXXXXXXXXXXXXXXX was unsuccessful");
  return content;
}
