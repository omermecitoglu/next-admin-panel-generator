import "server-only";

export async function insert{{ pascalCase modelName "singular" }}(requestBody: New{{ pascalCase modelName "singular" }}DTO) {
  const { success, content } = await XXXXXXXXXXXXXXXXXXXXXX(requestBody);
  if ("errorCode" in content) {
    switch (content.errorCode) {
      case "BAD_REQUEST":
      case "UNKNOWN_ERROR":
        throw new Error(content.errorCode);
    }
  }
  if (!success) throw new Error("XXXXXXXXXXXXXXXXXXXXXX was unsuccessful");
  return content;
}

export async function patch{{ pascalCase modelName "singular" }}({{ camelCase modelName "singular" }}Id: {{ pascalCase modelName "singular" }}DTO["id"], requestBody: {{ pascalCase modelName "singular" }}PatchDTO) {
  const { success, content } = await XXXXXXXXXXXXXXXXXXXXXX({{ camelCase modelName "singular" }}Id, requestBody);
  if ("errorCode" in content) {
    switch (content.errorCode) {
      case "XXXXXXXXXXXXXXXXXXXXXX":
      case "UNKNOWN_ERROR":
        throw new Error(content.errorCode);
    }
  }
  if (!success) throw new Error("XXXXXXXXXXXXXXXXXXXXXX was unsuccessful");
  return content;
}

export async function delete{{ pascalCase modelName "singular" }}({{ camelCase modelName "singular" }}Id: {{ pascalCase modelName "singular" }}DTO["id"]) {
  const { success, content } = await XXXXXXXXXXXXXXXXXXXXXX({{ camelCase modelName "singular" }}Id);
  if (content && "errorCode" in content) {
    switch (content.errorCode) {
      case "XXXXXXXXXXXXXXXXXXXXXX":
      case "UNKNOWN_ERROR":
        throw new Error(content.errorCode);
    }
  }
  if (!success) throw new Error("XXXXXXXXXXXXXXXXXXXXXX was unsuccessful");
  return content;
}
