import "server-only";
import { {{ camelCase modelName "singular" }}CT, {{ camelCase modelName "singular" }}IdsCT } from "~/core/cache";

async function getAll{{ pascalCase modelName "singular" }}Ids() {
  const { success, content } = await XXXXXXXXXXXXXXXXXXXXXX({{ camelCase modelName "singular" }}IdsCT(), { idsOnly: true });
  if ("errorCode" in content) {
    switch (content.errorCode) {
      // eslint-disable-next-line @typescript-eslint/no-unnecessary-condition
      case "VALIDATION_ERROR":
        throw new Error(JSON.stringify(content.detail));
    }
  }
  if (!success) throw new Error("getAll{{ pascalCase modelName "singular" }}Ids was unsuccessful");
  return content;
}

export async function getSingle{{ pascalCase modelName "singular" }}({{ camelCase modelName "singular" }}Id: {{ pascalCase modelName "singular" }}DTO["id"]) {
  const { success, content } = await XXXXXXXXXXXXXXXXXXXXXX({{ camelCase modelName "singular" }}CT({{ camelCase modelName "singular" }}Id), {{ camelCase modelName "singular" }}Id);
  if ("errorCode" in content) {
    switch (content.errorCode) {
      case "XXXXXXXXXXXXXXXXXXXXXX":
        throw content.errorCode;
      case "VALIDATION_ERROR":
        throw new Error(JSON.stringify(content.detail));
    }
  }
  if (!success) throw new Error("getSingle{{ pascalCase modelName "singular" }} was unsuccessful");
  return content;
}

export async function getAll{{ pascalCase modelName "plural" }}() {
  const list = await getAll{{ pascalCase modelName "singular" }}Ids();
  return await Promise.all(list.map(item => getSingle{{ pascalCase modelName "singular" }}(item.id)));
}

export function getSpecific{{ pascalCase modelName "plural" }}(ids: {{ pascalCase modelName "singular" }}DTO["id"][]) {
  return Promise.all(ids.map(id => getSingle{{ pascalCase modelName "singular" }}(id)));
}
