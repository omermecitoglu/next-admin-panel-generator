import "server-only";

function getAll{{ pascalCaseSingular }}Ids() {
  // make your api calls here
  return 
}

export function getSingle{{ pascalCaseSingular }}({{ camelCaseSingular }}Id: {{ pascalCaseSingular }}DTO["id"]) {
  // make your api calls here
  return 
}

export async function getAll{{ pascalCasePlural }}() {
  const {{ camelCaseSingular }}List = await getAll{{ pascalCaseSingular }}Ids();
  return await Promise.all({{ camelCaseSingular }}List.map(u => getSingle{{ pascalCaseSingular }}(u.id)));
}

export function getSpecific{{ pascalCasePlural }}(ids: {{ pascalCaseSingular }}DTO["id"][]) {
  return Promise.all(ids.map(id => getSingle{{ pascalCaseSingular }}(id)));
}
