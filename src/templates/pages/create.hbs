import PageTitle from "@omer-x/bs-ui-kit/PageTitle";
{{#if i18n}}
import { getTranslations, setRequestLocale } from "next-intl/server";
import type { AppLocale } from "~/i18n/routing";
{{/if}}
import { create } from "~/actions/{{ kebabCaseSingular }}";
import BackButton from "~/components/BackButton";
import {{ pascalCaseSingular }}Form from "~/components/admin/{{ kebabCasePlural }}/{{ pascalCaseSingular }}Form";

type Create{{ pascalCaseSingular }}PageProps = {
  {{#if i18n}}
  params: Promise<{ locale: AppLocale }>,
  {{else}}
  params: Promise<unknown>,
  {{/if}}
};

const Create{{ pascalCaseSingular }}Page = async ({
  params,
}: Create{{ pascalCaseSingular }}PageProps) => {
  {{#if i18n}}
  const { locale } = await params;
  {{else}}
  const pageParams = await params;
  {{/if}}
  {{#if i18n}}
  setRequestLocale(locale);
  const t = await getTranslations("{{ pascalCaseSingular }}");
  {{/if}}
  return (
    <>
      <PageTitle name="New {{ capitalCaseSingular }}">
        <BackButton fallback="/admin/{{ kebabCasePlural }}" />
      </PageTitle>
      <{{ pascalCaseSingular }}Form action={create} />
    </>
  );
};

export default Create{{ pascalCaseSingular }}Page;
