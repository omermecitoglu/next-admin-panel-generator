import PageSection from "@omer-x/bs-ui-kit/PageSection";
{{#if i18n}}
import { getTranslations, setRequestLocale } from "next-intl/server";
import type { AppLocale } from "~/i18n/routing";
{{/if}}
import { update } from "~/actions/{{ kebabCaseSingular }}";
import BackButton from "~/components/BackButton";
import {{ pascalCaseSingular }}Form from "~/components/admin/{{ kebabCasePlural }}/{{ pascalCaseSingular }}Form";
import { getOne{{ pascalCaseSingular }} } from "~/datasource/{{ kebabCasePlural }}";

type Edit{{ pascalCaseSingular }}PageProps = {
  {{#if i18n}}
  params: Promise<{ locale: AppLocale, id: string }>,
  {{else}}
  params: Promise<{ id: string }>,
  {{/if}}
  searchParams: Promise<{ r: string }>,
};

const Edit{{ pascalCaseSingular }}Page = async ({
  params,
  searchParams,
}: Edit{{ pascalCaseSingular }}PageProps) => {
  {{#if i18n}}
  const { locale, id: {{camelCaseSingular}}Id } = await params;
  {{else}}
  const { locale, id: {{camelCaseSingular}}Id } = await params;
  {{/if}}
  const { r } = await searchParams;
  {{#if i18n}}
  setRequestLocale(locale);
  const t = await getTranslations("{{ pascalCaseSingular }}");
  {{/if}}
  const fromList = r === "list";
  const {{ camelCaseSingular }} = await getOne{{ pascalCaseSingular }}({{camelCaseSingular}}Id);
  return (
    <PageSection
      title="Edit {{ capitalCaseSingular }}"
      toolbar={(
        <BackButton fallback={fromList ? "/admin/{{ kebabCasePlural }}" : `/admin/{{ kebabCasePlural }}/${ {{~camelCaseSingular~}} .id}`} />
      )}
    >
      <{{ pascalCaseSingular }}Form
        action={update.bind(null, {{camelCaseSingular}}.id, fromList)}
        data={ {{~camelCaseSingular~}} }
      />
    </PageSection>
  );
};

export default Edit{{ pascalCaseSingular }}Page;
