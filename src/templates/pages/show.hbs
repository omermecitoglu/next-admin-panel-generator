import { faPencil } from "@fortawesome/free-solid-svg-icons/faPencil";
import InfoTable from "@omer-x/bs-ui-kit/InfoTable";
import LinkButton from "@omer-x/bs-ui-kit/LinkButton";
import PageSection from "@omer-x/bs-ui-kit/PageSection";
{{#if i18n}}
import { getTranslations, setRequestLocale } from "next-intl/server";
{{else}}
import Link from "next/link";
{{/if}}
import BackButton from "~/components/BackButton";
import { getSingle{{ pascalCaseSingular }} } from "~/data/{{ kebabCasePlural }}";
{{#if i18n}}
import { type AppLocale, Link } from "~/i18n/routing";
{{/if}}

type Show{{ pascalCaseSingular }}PageProps = {
  {{#if i18n}}
  params: Promise<{ locale: AppLocale, id: string }>,
  {{else}}
  params: Promise<{ id: string }>,
  {{/if}}
};

const Show{{ pascalCaseSingular }}Page = async ({
  params,
}: Show{{ pascalCaseSingular }}PageProps) => {
  {{#if i18n}}
  const { locale, id: {{camelCaseSingular}}Id } = await params;
  {{else}}
  const { locale, id: {{camelCaseSingular}}Id } = await params;
  {{/if}}
  {{#if i18n}}
  setRequestLocale(locale);
  {{/if}}
  const [
    {{#if i18n}}
    t,
    {{/if}}
    {{ camelCaseSingular }},
  ] = await Promise.all([
    {{#if i18n}}
    getTranslations("{{ pascalCaseSingular }}"),
    {{/if}}
    getSingle{{ pascalCaseSingular }}({{camelCaseSingular}}Id),
  ]);
  return (
    <PageSection
      title={ {{~camelCaseSingular~}} .name}
      toolbar={(
        <>
          {perms.includes("can_create_{{ snakeCasePlural }}") && (
            <LinkButton
              as={Link}
              icon={faPencil}
              {{#if i18n}}
              text={t("edit.title")}
              {{else}}
              text="Edit {{ capitalCaseSingular }}"
              {{/if}}
              href={`{{ subPath }}/{{ kebabCasePlural }}/${ {{~camelCaseSingular~}} .id}/edit`}
              size="sm"
            />
          )}
          <BackButton fallback="{{ subPath }}/{{ kebabCasePlural }}" />
        </>
      )}
    >
      <InfoTable
        source={ {{~camelCaseSingular~}} }
        primaryKey="id"
        schema=\{{
          id: {
            header: "ID",
            long: true,
          },
        }}
      />
    </PageSection>
  );
};

export default Show{{ pascalCaseSingular }}Page;
